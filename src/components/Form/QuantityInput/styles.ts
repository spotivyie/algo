import styled from 'styled-components'

export const Container = styled.div`
  padding: 8px;
  border-radius: 6px;
  display: flex;
  gap: 10px;

  button {
    border: none;
    background-color: transparent;
    display: flex;
    align-items: center;
    cursor: pointer;
  }

  span {
    padding-top: 2px;
  }
`
